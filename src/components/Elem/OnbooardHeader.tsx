import { FC } from "react";

type Props = {
  header: string;
  paragraph: string;
};

const OnboardHeader: FC<Props> = ({ header, paragraph }) => {
  return <div></div>;
};

export { OnboardHeader };
